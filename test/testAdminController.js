const expect = require('chai').expect;
//const sinon = require('sinon');

//const WeatherDataPoint = require('../models/weatherDataPoint');
const adminController = require('../controllers/adminController');

describe('Test Admin Controller - version', function () {
    it('Should return version 1.0', function() {
        //sinon.stub(WeatherDataPoint, 'findOne');
        //WeatherDataPoint.findOne.throws();

        

        //adminController.getVersion(req, res, () => {});
        //console.log(res);
        //expect(response.body, JSON.stringify({ version: '1.0' }));

        //expect(adminController.getVersion(({}, {}, {}) => {}), JSON.stringify({ version: '1.0' }));
        
        //WeatherDataPoint.findOne.restore();
    });
});